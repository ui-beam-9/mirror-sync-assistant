# ====================================
# 企业微信应用配置
# ====================================

# 企业微信 CorpID（企业 ID，在企业微信管理后台的"我的企业"中获取）
WECOM_CORP_ID=your_corp_id_here

# 企业微信应用 AgentId（应用 ID，在应用详情页面获取）
WECOM_AGENT_ID=1000002

# 企业微信应用 Secret（应用密钥，在应用详情页面获取）
WECOM_SECRET=your_app_secret_here

# 企业微信 Token（自定义字符串，用于接收消息验证，至少 32 位）
WECOM_TOKEN=your_random_token_at_least_32_chars

# 企业微信 EncodingAESKey（消息加解密密钥，43位字符，可随机生成）
WECOM_ENCODING_AES_KEY=your_encoding_aes_key_43_characters_here

# 企业微信 API 地址
# 官方地址: https://qyapi.weixin.qq.com
# 注意：企业微信有"企业可信IP"限制，只有指定的 IP 地址能够调用 API
# - 如果服务器是【固定公网 IP】：直接将 IP 加入可信列表，使用官方地址即可
# - 如果服务器是【动态公网 IP】：需要一台固定公网IP的服务器反代企业微信 API，然后填写反代地址
WECOM_API_BASE=https://qyapi.weixin.qq.com

# ====================================
# GitHub 配置
# ====================================

# GitHub Personal Access Token
# 需要具有 repo 权限，用于创建 Issues
# 获取地址: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_github_token_here

# GitHub 仓库（格式: owner/repo）
# 例如: ui-beam-9/docker-images-sync-huaweicloud-swr
GITHUB_REPO=owner/repo

# ====================================
# 服务器配置
# ====================================

# 服务器监听端口
PORT=8080

# Webhook 验证密钥（用于 GitHub Actions 调用通知接口时验证身份）
# 请生成一个随机字符串，至少 32 位，并在 GitHub Secrets 中同步配置
WEBHOOK_SECRET=your_random_webhook_secret_at_least_32_chars
